<%= form_for(@rating_value) do |f| %>
  <% if @rating_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rating_value.errors.count, "error") %> prohibited this rating_value from being saved:</h2>

      <ul>
      <% @rating_value.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
